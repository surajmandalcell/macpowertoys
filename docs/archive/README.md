# Archived product notes

These documents record early product exploration and are not current
implementation guidance. Use `AGENTS.md`, `DESIGN.md`, and the active request
lists under `spec/` as the sources of truth.
