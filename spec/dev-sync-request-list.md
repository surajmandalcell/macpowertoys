# Dev Sync Request List

Reviewed against `spec/cloud-sync-dev-sync-spec.md` and current source on
2026-09-05. Update a status only after checking current source and, for
visible behavior, the latest normal signed build. Scenario numbers refer to
the scenario catalog in the spec. The complete unit suite passed 859 tests
with one deliberate skip and zero failures on the tree at `e885f8c`, and the
fifth signed live check on that build passed all 12 steps.

| Status | Request | Evidence | Remaining work |
|---|---|---|---|
| Done | Add Dev One-Way and Dev Bidirectional as explicit Cloud Sync destinations. | `b1dc6bc` adds the `Dev Sync` sidebar row with an attention badge, the `.devSync` route, the pair page, the six-step setup sheet, project rows, conflict cards, and the pair settings page. `DevSyncMode` carries the exact spec copy. 17 interface tests pass. The fifth signed live check on `e885f8c` opened the sidebar row, the `No Dev Sync pair yet` empty state, and every step of the setup sheet with Dev One-Way selected. | None. |
| Verify | Keep existing Copy, Sync, and Move transfers unchanged. | Dev Sync uses its own `DevSync/` state directory, its own `rsync` process, and joins the Cloud Sync runtime-need predicate only through `hasActivePairs` (`9efc61d`). All 543 pre-existing tests still pass inside the 859-test suite, and scenario 88 proves a queued rclone job stays untouched while a pair runs. Five signed live checks left the persisted transfer list and the rclone daemon untouched. | Confirm an existing transfer row and the Activity ledger visually in the installed build with a real transfer present. |
| Done | Show a mandatory first-run preview before any mutation. | `DevSyncService.preview` runs discovery, policy, scans, and both planners on an in-memory pair; service tests assert directory listings are identical before and after. `createPair` requires `scanComplete` and the approved preview. The Preview step titles its primary action from `DevPlanSummary.primaryActionTitle`. In the signed `e885f8c` build the preview listed 3 projects to mirror, 1 managed link, 126 files, and six sensitive files before any copy, and the primary action read `Copy 126 files and 1 managed link`. | None. |
| Done | Use Git as the source of truth for ignore rules. | `DevGit.workingTreeManifest` uses `git ls-files -z --cached --others --exclude-standard` including initialized submodules; `ignoredPaths` uses `git check-ignore --stdin -z`; every call sets `GIT_OPTIONAL_LOCKS=0`. 44 Git, discovery, and policy tests pass, including scenario 24. | None. |
| Done | Include ignored sensitive files by default. | `DevFilePolicyEngine` applies the sensitive override above Git ignore with a size guard (scenario 25). `24be686` emits the unencrypted-drive warning. | None. |
| Done | Copy Git administration data with active-lock safety. | `DevGit.activeLocks` and `isTransientGitPath` exclude transient locks; the engine defers the batch while a lock exists (scenario 26). | Hardening may narrow the deferral to Git metadata paths. |
| Done | Debounce FSEvents and collapse event storms. | `DevDirtyScheduler` implements the sliding quiet window, checkpoint, storm collapse, path collapse, manual retries, and persisted generations (scenarios 22, 23, 24, 83, 86). 32 event and scanner tests pass. | None. |
| Done | Never start one `rsync` per path. | Manifest batches per project; `DevVolumeMutationGate` serializes one mutation per external volume identifier; Sync Now merges into the next generation. | None. |
| Done | Give external-only projects managed internal links. | `DevManagedLinkManager` creates, validates, repairs, adopts, and removes links (scenarios 2, 6, 55, 66, 67, 68). 16 link and residency tests pass. | None. |
| Done | Never follow managed links during scanning or transfer. | Discovery and the scanner use `lstat` semantics and skip managed link paths; `DevRsyncTransfer.validateManifest` rejects link traversal and system paths. | None. |
| Done | Survive mount-path changes through volume identity. | Roots store `uuid:` identifiers; `repair` rewrites link text only for the same volume (scenario 55). | None. |
| Done | Block transfer on a wrong volume. | `DevSyncRoots.resolve` returns `identityMismatch`; the engine sets `.blocked` with a wrong-drive detail (scenario 54). | None. |
| Done | Use a stored baseline for bidirectional decisions. | Per-project baseline documents load and save 100,000 entries in about 0.9 s; the planner implements every row of the bidirectional table. 89 planner tests pass. | None. |
| Done | Turn simultaneous changes into conflicts. | Content, add/add, delete/modify, modify/delete, type, collision, identity, path, rename, and drift conflicts are planned with both signatures; conflict cards offer the six resolutions (scenario 27). | None. |
| Done | Require a complete healthy scan before any deletion. | The planner emits deletions only when both scans are complete and both roots were available; incomplete scans set `deletionsAllowed = false` (property tests 3 and 5). | None. |
| Done | Never propagate whole-project deletion automatically. | Missing internal projects convert to external-resident in Dev One-Way and wait for `decideMissingProject` in Dev Bidirectional (scenarios 7, 8). | None. |
| Done | Retain every destructive change. | Overwrites use `--backup-dir`; deletions and retired projects move into history through `DevSafetyStore`; retention keeps unresolved conflicts and open operations (scenarios 31, 49, 65). 29 store and safety tests pass. | None. |
| Done | Never advance an unverified baseline after interruption. | The operation runner commits only verified paths and marks interrupted operations recovery-required; `DevRecovery` inspects the journal at launch (scenarios 53, 79, 80). | None. |
| Done | Block affected projects on case and Unicode collisions. | The scanner detects collisions from raw UTF-8 bytes before building its dictionary; the planner blocks the project (scenarios 35, 36). | None. |
| Done | Let file-system capabilities control metadata flags. | `DevVolumeProbe` runs real round trips; `DevRsyncOptions.resolve` emits `-p`, `-X`, `-H`, and `--crtimes` only when proven (scenarios 42, 43, 59, 60, 75, 76). 23 roots and rsync tests pass. | None. |
| Done | Never build a shell command string. | Every process uses `Process` with `executableURL` and an argument array; no `/bin/sh` appears in the Dev Sync sources. | None. |
| Done | Pass unusual names through NUL manifests. | `--files-from=- -0` with sorted byte-order manifests; a real transfer of the path matrix through openrsync keeps names, symlinks, and executable bits (scenarios 37, 38). | None. |
| Done | Probe `rsync` capabilities instead of assuming them. | `DevRsyncProbe` parses `--help`, runs a self-test, and records a fingerprint; the local openrsync rejects `-X` in the self-test, so xattrs are disabled by evidence. | None. |
| Done | Pass the failure and acceptance matrices. | Path matrix (`9aa3d99`), Git matrix (`12c335e`), file-system matrix on real APFS case-insensitive and case-sensitive images with read-only, low-space, and remount cases (`61b8b3d`), the fifteen-step failure-injection matrix (`2c3a912`), scale (`1559447`), and the log redaction audit (`200179b`). Measured on this Mac: a 100,000-file repository scans in 24.8 s and plans in 2.5 s; 10,000 events collapse to one reconciliation in 0.031 s; a 1 GiB file hashes in 0.59 s with a 0-byte resident-memory delta after the bounded-buffer fix. The complete suite passed 847 tests with one deliberate skip. | exFAT is covered only by a synthetic capability record because this macOS build refuses to create an exFAT image. Low space is simulated through the reserve, not by filling the volume. Forced SIGKILL and destination mutation during copy have partial coverage. The hardening worker reported no global-ignore isolation test; the Git-ignore fix task adds one. |
| Done | Never copy a path that only the project `.gitignore` ignores. | `a3bbecf` feeds the Git working-tree manifest into the policy in the pair engine, the setup preview, and deep verification; a path inside a Git project that is outside the manifest and outside `.git` is Git-ignored and an ignored directory is not descended. A fixture with `secrets/` and `*.log` ignored copies neither through the real engine plan, and an isolated global-excludes test passes. `b0bfd16` commits dangling symlinks after an openrsync partial exit. 65 tests pass across the six affected classes. | None. |
| Done | Distinguish mirrored and external-resident projects in the UI. | Project rows show residency and state badges with SF Symbols and text, plus Open Real Location. The signed `e885f8c` build showed three `Mirrored` rows and one `External` row after the first run, `Changes pending` then `Clean` for the edited project, `Drift` with 1 path for the externally edited project, and `Offline` then `External` across the eject and remount. | None. |
| Done | Fix every functional defect from the first installed live check. | The first signed live check on `eb46dc9` found a first-run preview that reported nothing to change, a pair blocked by a false full-disk reading, no managed link for the external-only project, and false drift after the first copy. `438aef1` plans a pending mirror against an empty destination, counts managed links and sensitive paths, reads the plain available capacity when the important-usage value is zero, creates managed links at every catalog refresh while keeping an existing internal item, and raises the pair timestamp tolerance to one second for whole-second `openrsync` copies. A headless run of the real `DevSyncService` on the live fixture previewed 126 copies, 1 managed link, and 6 sensitive files, then finished with four clean projects and a healthy link. Four regression tests cover the preview, the capacity fallback, link creation with an occupied internal path, and the tolerance. | None. |
| Done | Keep the running pair reacting after the first run and keep the window current. | The second signed live check on `438aef1` copied the first run correctly but then froze: the page stayed on Planning, an internal edit never reached the drive, external drift never showed, and the eject never showed Drive offline. Two causes. The scheduler requeued the periodic generation of the external-resident project every 200 ms because it can never be planned, and its earliest event time made it win every turn, so no other project ran. The service also exposed one single-consumer update stream, and the window's task re-subscribed on every appearance, which silenced the interface. `66af20f` drops an unplannable generation instead of requeueing it, gives every subscriber its own update stream, consumes the self-event ledger at event intake so Cloud Sync's own writes no longer queue work, publishes the drift count on the no-mutation path, adds a Projects to mirror row to the preview, and gives every Rules switch one trailing control column. On the live fixture the internal edit reached the drive 19 s after the save and external drift showed within 1 s of the write. A starvation regression test covers the scheduler. | None. |
| Done | Show pending changes and drive-offline state on project rows. | The third signed live check on `182a2dd` passed 10 of 12 steps: the first run, the internal edit reaching the drive, drift with a count of 1, the eject and remount, and removal with safety-store deletion all passed. Two rows stayed stale: a clean project stayed `Clean` while its edit waited for the quiet window, and the external-resident project stayed `Clean` while the drive was offline. `586600e` marks a project `Changes pending` from the first accepted event and derives every external-resident row from its link state, so it shows `Offline` after the unmount and returns to `Clean` after the remount. A pair-engine test covers both transitions. | None. |
| Done | Show each project's drift paths on its row and keep the pair count as the sum. | The fourth signed live check on `93add00` passed 11 of 12 steps: first run, pending state at 10 s and clean at 20 s with the marker copied, Drive offline at 10 s and Idle 10 s after the remount, and removal with safety-store deletion. The app-b row showed `Drift` without a count because drift paths only loaded when the row was previewed, and the status card showed 0 after the remount because the last project to run overwrote the pair count. `b44d35a` publishes each project's drift paths through a drift update as soon as the engine finds them and keeps the pair count as the sum over projects. | None. |
| Verify | Sync everything under the root with no project picker. | The owner asked for every file and folder, including `_docs`, `_`-prefixed folders, loose files, `.claude`, `.codex`, `.agents`, and configs, with only junk skipped. `2a05ed5` adds the root unit `Everything else` that covers all non-repository content, carves nested repositories and links out of its scans, routes its events to a limited reconcile plus discovery, and removes the include switches from setup. Git repositories stay units. Tests cover the root unit copy, nested-unit carve-out, and catalog order. | Run the everything-mode live check on the installed build. |
| Verify | Make the skip list the only filter, with Git-tracked content winning. | The owner chose: skip list only, Git ignore off, tracked content in `vendor/` or `build/` still syncs, `dist` kept, editor temp and logs kept, `tmp` skipped at any depth, `.venv`, `Pods`, `vendor`, and Carthage skipped. `2a05ed5` expands the built-in list across JavaScript, Python, JVM, Apple, Flutter, and other ecosystems, drops `*.swp` and `*~` from the hard exclusions, removes `dist` from the build-output list, and defaults `followGitIgnore` off and `skipUnignoredBuildOutputs` on. `testEverythingModeSkipListIsTheOnlyFilter` proves each rule. | Confirm on the installed build that `node_modules`, `tmp`, and `.venv` never reach the drive while `dist` and `_docs` do. |
| Verify | Link drive-only folders at the same internal path. | The owner confirmed: a folder that exists only on the drive, such as `organization/lambton/meallens/meallens-data`, gets a symlink at the same place on the Mac. `2a05ed5` makes the external walk report the shallowest drive-only directory outside every repository, the catalog turns it into a linked unit when the root baseline does not know it, and the engine creates the link. A deleted-on-Mac folder stays a mirror deletion. Discovery, catalog, and engine tests cover it. | Create a drive-only folder during the live check and confirm the link. |
| Done | Explain how to add a file policy or an `rsync` capability without changing reconciliation. | `docs/DEV_SYNC.md` (`d79127f`). | None. |
