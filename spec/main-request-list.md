# Main Task Request List

Reviewed against current source and Git history on 2026-08-31. This list covers
requests made in the original main task. Dedicated applet tasks own their own
lists where noted below.

Update a status only after checking current source and, for visible behavior,
the latest normal signed build.

## Open or Needs Verification

| Status | Request | Current evidence | Remaining work |
|---|---|---|---|
| Done | Make the main MacPowerToys window wide enough to hold four tool cards in one row. | `c2a4277` sizes the launcher content at 1,200 x 720 through the shared `UtilityLayout.launcherContentSize`, and `MainWindowView`, the `main` scene `defaultSize`, and the `main` minimum content size all read that one constant. The grid reads `launcherCardMinimumWidth` 220, `launcherGridSpacing` 16, and `launcherContentInset` 24, so the 980pt content pane needs 976pt for four columns and cannot fit a fifth. `testLauncherContentPaneFitsFourToolCardsInOneRow` asserts both bounds. The launcher also joins the position-only restore set, and `testLauncherRestoresPositionOnlySoAnOldSavedSizeCannotReturn` proves a saved 780 x 732 frame reopens at the current size. The Debug `7d41ab0` build measured the window at 1,200 x 752 (720 content plus the 32pt titled surplus) and showed four cards in each of the three rows; `docs/screenshots/macpowertoys-launcher.png` is that capture. In the installed signed `e41ef3c` build, whose launcher source is unchanged at `750b1d1`, the Codex-driven check measured a 1,200 x 752 outer frame (1,200 x 720 content), a 220pt sidebar, and four equal 221pt cards per row with 16pt gaps. | None. |
| Done | Remove every version number and online indicator from every sidebar. | `7d41ab0` deletes the Cloud Sync daemon version string and its green, orange, and red status dot, keeping only the title and the `Reconnect` recovery action, and deletes the System Monitor sidebar sampling dot and caption. A repeat grep of every sidebar view for `CFBundleShortVersionString`, a version label, and green, orange, or red `Circle()` fills returns nothing. App Settings keeps `Version` because it is not a sidebar. Operational state stays in content rows. In the Debug `7d41ab0` build the Cloud Sync sidebar title showed no dot and no version. The installed signed `e41ef3c` build showed no daemon version or status dot in the Cloud Sync sidebar, and `76c59fe` also removed the `Connected · v1.75.0` line and green dot from the Cloud Sync menu-bar tab. | None. |
| Done | Make Command-Q close only the active sub-app and protect launcher quit with a repeat press. | The standard app termination command now routes through one AppDelegate target. Known SwiftUI sub-app scopes close their visible windows through native `performClose`; sheets resolve through their parent chain and end before their tool windows close. Ruler closes Settings, Defaults, and every managed ruler through `RulerManager`, then stops its mouse timer. Main, unknown, and missing window scopes require two Command-Q presses within two seconds and show a visible repeat prompt. `3952bee` adds a direct close-path matrix for all ten native sub-app and Settings identifiers, proves sibling isolation, runs modal-sheet teardown through a real parent and sheet, and proves that a press after the two-second deadline starts a new protected window. It also proves that Command-W keeps the native window-close command. A mutation that rejected every tool window failed all 20 expected close assertions. The exact installed `c797014` build confirmed one Command-Q closes AI History, Cloud Sync, Logs, Ruler, Awake, Color Picker, Text Extractor, Input Devices, System Care, System Monitor, and NetToys while the main process stays alive. The exact signed `5a3f745` soak completed 275 Command-W closes across all 11 surfaces with zero close errors. No command-routing source changed after the live matrix. | None. |
| Done | Show one visible close control in every modal, including App Settings. | One shared `UtilityModalCloseButton` now appears in all 18 SwiftUI sheet presentations, including the Tailscale peer picker and SSH key-access sheet, plus the custom centered modal and inline AI History settings surface. Native alerts, confirmation dialogs, file panels, popovers, and Ruler settings keep their native dismiss controls. | None. The two SSH Anchor live states are owned by `spec/system-tools-request-list.md`. |
| Done | Keep every workspace sidebar flush with its opaque body. | NetToys no longer inserts its unique divider between the zero-spacing sidebar and body. The other seven sidebar workspace roots already place both panes directly together. The exact signed installed `2b3c309` build showed one continuous boundary in the launcher, AI History, Cloud Sync, Logs, Input Devices, System Care, System Monitor, and NetToys. No window background appeared between either pane. | None. |
| Done | Use thin overlay scroll indicators in every app and sub-app. | Every one of the 55 SwiftUI `ScrollView`, `Form`, `TextEditor`, `List`, and `Table` surfaces uses the shared modifier. Both native `NSScrollView` surfaces use the shared AppKit configuration. `7f0e5be` replaced the current 15pt native thumb with a 4pt thumb, `42be750` made the SwiftUI bridge target the matching sibling surface, and `99241f6` added explicit idle hiding, scroll and edge-hover reveal, a 0.9-second fade, Increased Contrast, and Reduced Motion behavior. Four focused tests pass. The signed installed `99241f6` build showed no idle launcher thumb, a thin thumb during scrolling, and no thumb after the fade. | None. |
| Done | Tighten shared launcher/workspace chrome and interaction. | Shared metrics place titles at 84pt and first content at 44pt. Launcher detail names use 17pt medium text, Open is regular size, tabs start at the 20pt edge, and tool rows open on double-click. Logs navigation keeps 12pt pane padding. The signed `a5ad439` build confirmed the launcher and every workspace sidebar; single-click selected Input Devices and double-click opened it. | None. |
| Done | Remove launcher category labels and use the selected Minimal page header. | The exact installed `abab22a` build showed one flat sidebar with all ten installed built-in tools and no category headings. Its All Tools grid kept every name and two-line description within the cards. A live accessibility sweep opened every built-in detail page, found the compact identity, action, and tab controls, and found no category label. No marketplace tool was installed to add another detail page. | None. |
| Done | Let menu-bar-capable tools choose None, Combined, or Separate. | Cloud Sync, Awake, Color Picker, Text Extractor, and Input Devices share one saved segmented launcher control and one placement predicate. Existing separate-icon choices migrate unchanged, and Cloud Sync and Awake keep their combined defaults. Five focused controller tests cover all 15 tool and mode pairs, disabled tools, exact stable autosave names, exact quick-action or open routes, combined-tab support, and no-op refresh plans that preserve existing native items. System Monitor keeps its specialized grouped and individual metric controls. Its focused test creates one grouped item, all seven individual items, and zero items; proves exact autosave names; and proves style or cadence changes keep the same item identities. | None. Physical behavior is owned by the consolidated menu-bar matrix below. |
| Done | Put almost all of each tool's settings in the menu bar. | `e5edb24` moves `ToolSettingsContent` out of `ToolAboutView` so the tray, the launcher detail, and each tool window render one implementation. Every combined tray tab now shows its operational summary and then that shared view inside one scrolling region capped at 70 percent of the screen. A `compactSettingsLayout` environment value adapts each view to 340pt instead of copying controls: the view drops its own scroll container, uses the 12pt tray gutter, Cloud Sync renders its sections without the grouped `Form`, and Awake stacks its interval and process rows. Input Devices reuses `InputDevicesSettingsView` where the launcher previously rendered `EmptyView`. Awake no longer repeats Keep Display On or the mode picker in the summary. Offscreen renders at 340pt in light and dark measured 1,213pt for Cloud Sync, 618pt for Awake, 619pt for Text Extractor, 951pt for Input Devices, and 398pt for Color Picker, each within the 70 percent cap on the 2,130pt test display. `TrayPopoverLayoutTests.testEveryTrayTabbedToolRendersItsSharedSettingsView` proves that none of the five falls back to the no-settings placeholder. In the installed signed `750b1d1` build, a native Accessibility driver clicked the status item and pressed each tab: the popover was 340pt wide, the body started 12pt under the 28pt tab row (8pt outer plus 4pt group inset), and the five tabs exposed 35, 28, 12, 16, and 22 live controls (Cloud Sync, Awake, Color Picker, Text Extractor, Input Devices), with the tallest tab at 998pt on a 1,600pt display. `76c59fe` removed the `Connected · v1.75.0` line and dot, shortened Awake preset chips to `15min 30min 1h 2h` so they no longer truncate at 340pt, and made the thin-scroll configurator skip the page scroll view so the ignore-pattern editor lost its legacy scroller; the `750b1d1` captures under the session scratchpad show all three. A synthetic Escape key did not close the popover while another window held key focus; a second status-item click closes it. | None. |
| Done | Keep combined menu-bar navigation compact and remember its selected tool. | The combined popover includes all five eligible tools, persists `tray.selectedTab`, and switches from icon-and-text tabs to icon-only tabs when more than two tools are combined. Color Picker and Text Extractor expose their quick actions; Input Devices exposes Open. Focused tests prove that a saved available tool stays selected and that an unavailable saved tool falls back to the first available tool. | None. Physical behavior is owned by the consolidated menu-bar matrix below. |
| Done | Keep launcher detail introductions compact at the top. | The shared settings body places every description and menu-bar selector directly below the tabs. The Input Devices-only flexible spacer that bottom-anchored its introduction was removed. The exact installed `94da191` build showed the compact introduction at the top of Cloud Sync, Awake, Color Picker, Text Extractor, and Input Devices. | None. |
| Done | Use large search fields only in sidebars and thin native search fields inside every app and sub-app. | The launcher, Logs, and AI History sidebars use the padded `SidebarSearchField`. AI History messages, Color Picker history, Cloud Sync files and connector picker, NetToys scanner and history, and System Care applications use the shared native small `NSSearchField` in a 24pt slot. The focused native-control regression test passes. The exact installed `94da191` build showed all ten search surfaces at their intended size. | None. |
| Done | Remove the large blue focus outline from custom controls in every app without disabling keyboard access. | Every plain and borderless SwiftUI control applies `focusEffectDisabled()`. `5ec34ce` closes the two remaining NetToys icon-button gaps, adds source coverage for every custom button style, and redirects initial workspace focus away from controls. The exact installed `50c8f69` build passed Tab traversal in the launcher and all ten built-in app windows. Space activated custom controls in each control family. Native editors kept the normal macOS focus ring. | None. Physical menu-bar behavior is owned by the consolidated matrix below. |
| Verify | Give every custom selectable item clear hover and pressed feedback. | One shared interaction style supplies a 0.06 primary hover fill and 0.1 pressed fill for the Tailscale peer chooser, System Care overview cards and folder rows, Color Picker tabs and project rows, Awake modes, transfer-detail tabs, AI History bookmark chips, Cloud connector rows, and combined tray tabs. `a95f66e` routes launcher cards, standard and external sidebar rows, Logs actions and filters, marketplace actions, Text Extractor actions, tray tabs and actions, and footer actions through a true pressed state while preserving native and destructive controls. `855a250` keeps informational System Care file rows inert. `a9c97a5` keeps a selected tray tab's accent opaque, adds 0.10 hover and 0.18 pressed overlays, and uses the contrast-aware selected-control foreground. Ten focused tray tests and the combined 543-test suite pass. | Inspect every updated control family in the exact installed build. |
| Verify | Keep short related metadata on one row and use multiple columns on sparse wide pages. | `a318b41` compacted System Care storage rows and made Settings adaptive. `4ad3da2` applied the same row rule to shared page strips, launcher details, device and history rows, Color Picker, Text Extractor, marketplace sources, cleanup, and System Monitor. `cdca5b1` corrects the System Monitor history grid to use two columns at the normal width and one at the minimum width. It also gives arbitrary marketplace URLs and cleanup scopes their own bounded, middle-truncated rows. The focused tests pass, including mutation checks against the old grid and horizontal path rows. | Inspect default and minimum widths in the latest normal installed build. Confirm that wide sparse pages use two columns, narrow pages fall back to one, and long text remains readable. |
| Done | Keep branded sidebar icons in their original colors when selected. | `SidebarRow` keeps custom artwork in original rendering mode. `81de8b1` removed the Logs-only light dark-mode tile that looked white in every selected and unselected use. The normal signed `631facd` build confirmed the dark branded Logs artwork while that launcher row was selected. | None. |
| Done | Keep every active tool icon on one rounded tile template at every size. | All active light and dark SVG appearances use the same 512pt tile, 112pt clip radius, and full-artwork clip. `3436328` makes the shared launcher, sidebar, grid, and tray renderer mask every icon with the exact 112/512 radius ratio. Source-template, transparent-corner, and shared-renderer regression tests pass. The signed installed `3436328` build showed matching rounded corners across every launcher sidebar and grid icon, including the System Monitor detail header that reproduced the defect. | None. |
| Done | Reduce structural divider strength everywhere without changing native menu separators. | All visual SwiftUI dividers route through the contrast-aware shared quiet divider at 0.22 opacity, or 0.44 with Increased Contrast. The post-change search leaves only the shared implementation and native command-menu or context-menu separators. The normal signed `1666d71` build confirmed the quieter divider in the launcher. | None. |
| Done | Keep workspace actions compact and aligned, and add restrained motion across apps. | `6247587` keeps every workspace action at its native width on one centered 24-point row and makes the subtitle yield first. `631facd` applies the same compact rule to every shared tool header. Every window applies the Reduce Motion policy. The normal signed builds confirmed complete controls across every System Care page at the compact limit and confirmed the animated bottom-edge status transition. The Reduce Motion regression test passes. | None. |
| Done | Put transfer priority on individual files instead of the whole transfer. | Exact file jobs show a native five-level priority menu. Queue sorting and snapshots apply priority only to file jobs. Directory jobs always use normal priority. A running file continues without interruption, and its new priority applies only if it enters the queue again. Focused model tests pass. | None for individually queued files. |
| Platform limit | Change the priority of an active file inside a directory transfer. | The official rclone order controls support name, size, or modification date before a directory transfer starts. rclone has no supported live per-file priority API. | Keep active files running unless rclone adds a supported API. |
| Done | Audit local changes, and audit both sides when a true two-way setup exists. | `LocalChangeHistory` records the latest 100 events per transfer from the watched local source. Cloud Sync has only one-way Copy, Sync, and Move modes. A future two-way mode must watch and label both local roots. | None for the current transfer modes. |
| Done | Keep menu-bar left-click native and remove the custom double-click action. | The status-item monitor now intercepts only right mouse-down. `MenuBarExtra` owns left-click and opens immediately. The tested `StatusItemClickCoordinator` remains dormant for a future item without a dropdown. | None. |
| Done | Give the menu-bar icon a separate right-click menu containing only Open and Quit with icons. | The status item intercepts right mouse-down and builds an exact two-item native menu with SF Symbol icons. The normal signed `acf327e` build exposed only Open MacPowerToys and Quit in the live accessibility tree. | None. |
| Done | Align and organize the menu-bar popover as navigation, tool controls, and footer. | `4177868` removed the 150pt tab cap and the 30pt wrapper that caused trailing and bottom slack. `e5edb24` then removed the space under the tab row: the group carried a 20pt top inset and a 6pt bottom inset while each body added its own 10pt top inset, so an offscreen render of the Color Picker tab measured 20 blank points above the group and 18 below it. The group now uses one 8pt outer inset above and below, `TrayPopoverLayout.bodyTopInset` is 0, and the same render measures 8 points above and 10 below (8pt inset plus 2 points of text ascent). Every body keeps the 12pt gutter and 14pt bottom inset; the footer keeps 8pt top and 10pt bottom insets. `TrayPopoverLayoutTests.testTrayBodyStartsOneOuterInsetBelowTheTabRow` measures the rendered ink bands, and it fails on the old insets. In the installed signed `750b1d1` build the Accessibility frames put the 28pt tab row at 12pt from the popover top and the body scroll area 12pt under it on every tab (8pt outer inset plus the 4pt group inset), with the footer actions 20pt high at the bottom edge. | None. |
| Done | Size the menu-bar popover to the active tab instead of stretching compact content to a display-height cap. | `ab81a67` removes the flexible root height cap. Cloud Sync still caps its own scrollable list at 70 percent of the display height. | None. Physical behavior is owned by the consolidated matrix below. |
| Done | Use a darker menu-bar popover with a slight border around the complete tab group. | The popover applies an appearance-aware black shade. The tab group uses a 4pt inset, concentric 12pt outer and 8pt inner radii, a quieter neutral 1pt stroke, and one accent selected tab. | None. Physical behavior is owned by the consolidated matrix below. |
| Verify | Verify the complete physical menu-bar experience in one matrix. | Source and deterministic coverage prove the five-tool None, Combined, and Separate modes; compact two-tool and icon-only three-tool navigation; saved selection; native item identity; keyboard access; Awake states; Cloud Sync Pause and Resume; compact content sizing; appearance-aware border and background; and shared body and footer rhythm. A read-only accessibility probe of exact installed `327ebb1` confirmed that the current desktop driver still exposes neither MacPowerToys status items nor the SystemUIServer menu-extra tree. | In one physical session, check all five tools across None, Combined, and Separate; two-tool and three-tool combined navigation; selection restore; separate-item Command-drag and relaunch restore; focus and keyboard use; light, dark, and Increased Contrast appearance; compact Awake, idle and overflowing Cloud Sync sizing; native clicks; and Cloud Sync Pause and Resume. |
| Platform limit | Restore every window to its last position, monitor, and Space. | `WindowStateManager` stores size, position, and display identity for every built-in window. `cf5c975` prevents cold-launch frame replacement. The normal signed build restored Input Devices, System Care, and System Monitor to their exact saved sizes and built-in display after a full quit and relaunch. Apple's supported state restoration can restore window configuration only by reopening windows that were open at quit. macOS exposes no public API for assigning an independently reopened window to a Space. | Keep the current position and monitor restoration. Do not use private window-server APIs or re-enable automatic window reopening. That behavior would reopen tools that the user closed. |
| Done | Keep recent applet titlebars compact, borderless, consistently aligned, and free of focus outlines. | The shared row gives the title and actions equal 24pt frames on a 22pt centerline. `WindowAccessor` restores the 6pt traffic-light shift after delayed layout and every key-window transition; regression coverage forces a late native reset and verifies recovery in all four applets. Controls use a 6pt radius, initial focus stays off controls, and titles start at 60pt after hiding zoom. | None. |
| Done | Always use the latest committed build across concurrent tasks. | The troubleshooting rule and `Makefile` reject dirty or stale installs. The exact signed `327ebb1` build was installed after 539 of 539 tests passed. `HEAD`, `origin/main`, the app, and its embedded helper all matched, and both signatures passed strict verification. The app stayed closed after later source work made that installation stale. | Repeat the same clean commit, signature, and embedded-source gate before opening any later build. |
| Open | Make the app production ready and publish the next release only at the end. | The public repository is live, but the release checklist is not complete. Version `1.7.1` is public, and later work remains under `Unreleased`. `docs/PUBLIC_RELEASE_CHECKLIST.md` still has unresolved distribution gates. | Close the audited gaps, run release checks, refresh screenshots if the UI changed, then version, tag, and push the next release. |

## Implemented or Resolved

| Status | Request | Evidence |
|---|---|---|
| Done | Calculate the transfer plan first and prevent retries or resumed bytes from inflating its total. | `CheckFirst` is enabled, planned progress is separate from network-attempt bytes, and retry baselines preserve only completed work. |
| Done | Recalculate a transfer without shrinking its original plan, increasing it only for newly found work. | `TransferJob.applyRecalculatedPlan` adds only positive deltas, and Transfer Details exposes Recalculate with an rclone dry-run explanation. |
| Done | Keep the latest 100 changed-file audit entries for each transfer. | `LocalChangeHistory` limits records per job to 100, and Transfer Details contains the Changes tab. |
| Done | Offer continuous sync beside Remove after a local-folder transfer completes. | Completed transfer rows show the continuous-sync control beside Remove. A local watcher waits for a 30-second quiet period before requeueing changed work. |
| Done | Credit rclone properly. | App Settings > About links to rclone, names its contributors, and links its MIT licence. |
| Done | Use the compact labels `Comparing`, right-side ETA, and one-line byte/file totals. | Transfer rows render `Comparing`, put ETA in the right state badge, and keep metrics fixed-size on one row. |
| Accepted | Explain the aggregate speed even when visible file rows do not sum to it. | The aggregate comes from rclone statistics and can include work not represented by the visible file subset. The user accepted this behavior. |
| Done | Open the launcher on All Tools instead of opening Cloud Sync by default. | `HomeView` initializes selection to `all-tools`; only the tray remembers its own selected tool tab. |
| Done | Make closing the main window after opening a tool configurable and default it off. | App Settings contains `Close MacPowerToys after opening a tool`; both launcher paths honor it. |
| Done | Compact the tray footer and give its bottom edge slightly more space. | `4177868` keeps compact icon buttons and uses an 8pt top inset with a 10pt bottom inset. |
| Done | Replace blurry scale effects with simple slide or color-and-shadow feedback. | The tray uses directional move transitions. No `scaleEffect` remains, and launcher cards only change background, border, and shadow. |
| Done | Sign local builds with the personal Apple Development identity and remove GitHub signing CI. | `Makefile` uses the configured development team. No GitHub Actions workflow builds, signs, or publishes the app. |
| Done | Keep the imported Raycast extension aligned with each local app install. | The Raycast build generates all eleven tool PNGs from the current app SVG assets. `make install` builds and reloads the imported extension without launching a closed Raycast app. `sync-icons.sh --check` passes, all current assets are byte-identical to source, and the installed package contains the twelve manifest commands, twelve command maps, `package.json`, and thirteen asset files. | None. Visible rendering is owned by the consolidated Raycast row below. |
| Done | Remove the legacy PowerToys installation after adopting MacPowerToys. | Only `/Applications/MacPowerToys.app` is installed. Legacy identity support remains solely for safe data migration and deep-link compatibility. |
| Done | Enforce one app process and one utility window. | `AppInstanceCoordinator` uses running-app checks plus a process lock, and `ToolActionRouter` raises an existing utility window before creating one. |
| Done | Remember window size, position, and monitor for every built-in app window. | All ten SwiftUI windows use `WindowAccessor` and stable `WindowStateManager` identifiers. Ruler uses its own persisted AppKit state. Space restoration remains listed above. |
| Done | Align launcher cards with the sidebar search start and remove the body vertical mismatch. | `6d69af9` aligned the main content start and grid gutter with the launcher chrome. |
| Done | Remove excess top space from Logs and use thin overlay scrollbars. | Logs begins directly under its compact header and configures a mini overlay scroller. |
| Verify | Show the current icons in Raycast and cold-launch every MacPowerToys sub-app without exposing the main window first. | The Raycast manifest contains twelve no-view launchers and no action commands. `b61b0b3` adds NetToys, and the extension build passes. `b2657c8` gives native SwiftUI sub-app links one routing path. `dbd57c3` closes an existing launcher before Ruler creates its AppKit window. `4e181f8` makes the main scene accept only its own external event. In the exact signed `4e181f8` build, three 5 ms cold-launch traces showed Ruler and no positive-size launcher window. A cold Input Devices trace also showed only Input Devices. The exact installed `77dc585` package contains all twelve command bundles and twelve byte-identical imported icons; `sync-icons.sh --check` also passes. | Inspect representative icons in Raycast and run the NetToys command from a cold app state. Confirm NetToys opens without a positive-size launcher window. |
| Done | Consolidate troubleshooting knowledge and always verify the latest normal build. | `spec/troubleshoot/troubleshoot.md` is the only index and routes current-build, UI, verification, and shared-worktree rules. |
| Done | Preserve unexplained changes from other tasks instead of reverting whole files. | `spec/troubleshoot/shared-worktree.md` now requires inspecting history, identifying and asking the owner when possible, and preserving unknown work without explicit approval. |
| Done | Update README screenshots and publish the 1.7.0 checkpoint. | `README.md` uses the refreshed launcher and Text Extractor screenshots; annotated tag `v1.7.0` is pushed. Later work belongs to the pending next release. |
| Done | Prevent unsigned UI-runner Gatekeeper dialogs and correct the claim that the runner was killed. | Verification rules prohibit launching unsigned UI runners and require distinguishing a runner bootstrap failure from a killed process or product failure. |

## Dedicated Applet Audits

- Cloud Sync: `spec/cloud-sync-request-list.md`
- Awake: `spec/awake-request-list.md`
- Color Picker: `spec/color-picker-request-list.md`
- Text Extractor: `spec/text-extractor-request-list.md`
- Ruler: `spec/ruler-request-list.md`
- Input Devices, System Care, System Monitor, and NetToys: `spec/system-tools-request-list.md`

The dedicated lists do not override newer cross-app requirements recorded in
this main list or in the troubleshooting index.
